import React from 'react';
import SwipeableTextMobileStepper from './SwipeableTextMobileStepper'; // Import your SwipeableTextMobileStepper component

function RecentsContent() {
    return <SwipeableTextMobileStepper />;
}

export default RecentsContent;